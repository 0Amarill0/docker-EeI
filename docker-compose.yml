services:
  linux:
    build: .
    image: linux-test
    command: tail -f /dev/null
    tmpfs:
      - /disco-temporal
    volumes:
      - compartido:/disco-constante
volumes:
  compartido:
